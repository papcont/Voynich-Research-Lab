# External repositories

Do not vendor these repos into the scientific core by default.

For replication: pin commit, verify license, clone into ignored `.external/`, freeze evaluation before target run, and never use an author's semantic glosses as independent ground truth.
